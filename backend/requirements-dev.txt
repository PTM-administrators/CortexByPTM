# Dipendenze solo per lo sviluppo/test, separate da requirements.txt (quelle
# servono anche in produzione, queste no) — installa con:
#   pip install -r requirements-dev.txt
# (richiede anche requirements.txt già installato)
pytest==9.1.1
httpx==0.28.1  # richiesto da fastapi.testclient.TestClient
